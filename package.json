{
  "name": "linecat",
  "version": "1.0.1",
  "description": "linecat is a tool trying to simplify android log analytics. It also can be used as a generic tool for line content reducer.",
  "bin": {
    "linecat": "./linecat"
  },
  "scripts": {
    "build": "tsc && find src -name '*.json'|xargs -I{} cp {} build "
  },
  "repository": "https://github.com/zhangruize/logdog",
  "author": "ruize",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.31"
  }
}